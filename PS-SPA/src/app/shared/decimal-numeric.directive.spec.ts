import { DecimalNumericDirective } from './decimal-numeric.directive';

describe('DecimalNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalNumericDirective();
    expect(directive).toBeTruthy();
  });
});
